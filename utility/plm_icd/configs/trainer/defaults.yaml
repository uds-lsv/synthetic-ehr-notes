epochs: 200
validate_on_training_data: true #Validate on the traing set in addition to on the validation set
print_metrics: true #Print results after each epoch
use_amp: true #Use mixed precision
threshold_tuning: true #Picks the optimal threshold based on the validation set



